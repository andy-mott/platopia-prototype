// Re-export from the canonical shared styles location.
// This proxy exists so experience files moved into category subfolders
// can still resolve "../shared/styles" without internal modifications.
export { COLORS, GRADIENTS, FONTS } from "../../shared/styles";
